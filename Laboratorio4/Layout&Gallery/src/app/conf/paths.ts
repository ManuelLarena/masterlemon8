import { AppRoutes } from '../model/App-Routes';

export const appRoutes: AppRoutes = {
  home: '/home',
  login: '/login',
  about: '/about',
  dashboard: '/dashboard',
  gallery: '/gallery',
  crud: '/crud',
  profile: '/profile',
};
